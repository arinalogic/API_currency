# Виртуальное окружение Python — создаётся локально у каждого разработчика.
venv/

# Локальный кэш курсов валют — программа создаёт файл при первом запуске.
currency_rate.json

# Скомпилированные файлы Python — создаются автоматически при запуске.
__pycache__/
*.pyc
